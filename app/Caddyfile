{$PTRAINER_DOMAIN:localhost} {
  encode zstd gzip

  header {
    Strict-Transport-Security "max-age=31536000; includeSubDomains"
    -Server
  }

  reverse_proxy app:4173 {
    health_uri /readyz
    health_interval 15s
    health_timeout 3s
  }

  log {
    output stdout
    format json
  }
}
